{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn scaffold_ai.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uv run uvicorn scaffold_ai.main:app --host 0.0.0.0 --port 8000",
    "test": "uv run pytest",
    "lint": "uv run ruff check src tests",
    "type-check": "uv run mypy src"
  }
}
