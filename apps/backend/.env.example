AWS_REGION=us-east-1
AWS_PROFILE=admin

# Deployment tier — controls model selection (testing/optimized/premium)
# testing:   claude-haiku-4-5   (fast, cost-effective — default)
# optimized: claude-sonnet-4-5  (balanced)
# premium:   claude-opus-4-5    (highest quality)
DEPLOYMENT_TIER=testing

# Model override — leave blank to use tier default
BEDROCK_MODEL_ID=

BACKEND_URL=http://localhost:8001
PORT=8001
